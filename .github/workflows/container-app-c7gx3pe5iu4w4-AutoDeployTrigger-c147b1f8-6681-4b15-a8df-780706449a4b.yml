name: Trigger auto deployment for container-app-c7gx3pe5iu4w4

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ main ]
    paths:
    - '//**'
    - '.github/workflows/container-app-c7gx3pe5iu4w4-AutoDeployTrigger-c147b1f8-6681-4b15-a8df-780706449a4b.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.CONTAINERAPPC7GX3PE5IU4W4_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.CONTAINERAPPC7GX3PE5IU4W4_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.CONTAINERAPPC7GX3PE5IU4W4_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }} 
          registryUrl: 
          registryUsername: ${{ secrets.CONTAINERAPPC7GX3PE5IU4W4_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.CONTAINERAPPC7GX3PE5IU4W4_REGISTRY_PASSWORD }}
          containerAppName: container-app-c7gx3pe5iu4w4
          resourceGroup: expresssamplebc65b0da
          imageToBuild: default/[parameters('containerAppName')]:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


